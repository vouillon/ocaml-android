
# Path of the Android NDK
ANDROID_NDK = ${HOME}/.opam/4.02.0/lib/android-ndk-linux

# OCaml sources (will be copied)
OCAML_SRC = ${HOME}/.opam/4.02.0/lib/ocaml-src

# Where the cross-compiler libraries are installed
ANDROID_PREFIX = ${HOME}/.opam/4.02.0/arm-linux-androideabi

# Where the cross-compiler binaries are installed
ANDROID_BINDIR = ${HOME}/.opam/4.02.0/bin
